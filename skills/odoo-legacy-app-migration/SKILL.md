---
name: "odoo-legacy-app-migration"
category: "DevOps"
description: "Use this skill when you need to migrate a personal or legacy Odoo module across multiple major versions (e.g. 12.0 to 19.0), one version branch at a time, verifying real installability and behavior instead of trusting manifest flags or changelogs. Covers per-version environment setup (git worktrees, pyenv, macOS/arm64 dependency substitutions), a verification ladder (pre-commit, scoped and full test runs, measured coverage, real headless-browser checks), a CI pipeline template that runs inside the official odoo/<version> Docker image and reports both test results and coverage, a table of recurring bug classes found this way, and PR/MR discipline. Triggers on: 'migrate this module to Odoo', 'upgrade this addon to version', 'port this app to the next Odoo version', 'audit this module for version', 'set up CI for this Odoo module'."
last_validated: "2026-08-09"
---

# Skill: Odoo Legacy App Multi-Version Migration

## Golden Rule

**A manifest's `installable: True`, a changelog line, or a passing-looking install log is not verification.** The only thing that counts: it installs cleanly on a real local instance of the *target* Odoo version, its automated tests pass (and you read the log, not just the exit code), coverage is *measured* rather than assumed, and a real headless browser sees the feature working with zero console errors on more than one page.

This skill was distilled from real multi-version migrations of personal Odoo apps, where every rule below caught a real bug or wasted real time before it was written down.

## Migration Strategy — One Version at a Time

1. **Work from the existing remote branch for that version if one exists** (`git checkout -b work-X.0 origin/X.0`). Never discard history and rebuild from scratch unless the user explicitly asks for it. A prior migration attempt that looks abandoned (`installable: False`, modules missing entirely) is a signal to *re-audit*, not to assume it's unsalvageable — bulk "disable everything, sort it out later" commits are common and usually disabled things that actually work fine.
2. **Audit every app present on that branch, not just the ones you started with.** Real repos grow: new apps get added on later version branches, and existing apps gain features across versions (a field added in one version update might already exist, silently unwired, on an earlier branch too — check, don't assume the changelog note is where a feature actually started). Every app/feature gets the same rigor regardless of where it came from.
3. **Per app, per version:** check the real state (don't trust the flag) → if it already installs and passes tests, add coverage rather than re-touching working code → if it's broken/disabled/missing, migrate/fix it.
4. **Before migrating any feature, check whether the target Odoo core version already covers the same use case natively.** If it does, don't resurrect the old code — grep the target core for the equivalent field/mixin, document the native equivalent in the PR description, and drop the feature. (Real example: a "multi-logo-per-website" app became pointless once core Odoo shipped a native per-website `logo` field — that's almost certainly *why* the original author dropped it without a replacement.)
5. **Never merge the working branch directly into the version branch.** Push it and open a Draft PR/MR for review. Leave a real local instance running afterward (see "Verification Ladder" step 5) so the requester can test it themselves without re-running anything.
6. **Modernize JS as part of the migration, don't just port it as-is.** Porting old frontend code forward one version at a time tends to carry the same legacy patterns through every branch untouched, because "it still works" isn't the same bar as "it's not what you'd write today." Each version bump is the moment to check what the *current* target version's JS framework actually recommends and move to it — don't wait for a future version to force the rewrite:
   - Raw inline `<script>` + `DOMContentLoaded` (+ jQuery global) → `publicWidget` (frontend) or an OWL component (backend/interactive UI), matching whichever the target version's own core modules use for the same kind of surface.
   - Legacy `odoo.define("module.name", function (require) {...})` module wrapper → the ES6 `/** @odoo-module **/` + `import`/`export` syntax once the target version supports it (Odoo 17+ mandates it; core drops `odoo.define` entirely there).
   - Old `Widget`-based backend UI components → `OWL` components once the target version's core has moved to OWL for that same class of UI (started 15.0, mandatory by 17.0) — don't leave a `Widget` subclass in a codebase whose own core equivalent has already moved on.
   - Direct jQuery DOM manipulation where the target version's core has already replaced the equivalent core widget with vanilla JS/OWL — prefer matching core's current approach over preserving the old jQuery call for its own sake.
   - As with any other feature (see step 4), check whether target core already exposes a tracking/analytics/UI hook natively before keeping a bespoke implementation of it.
   - This is a "same rigor as everything else" rule, not a free pass to rewrite for style: only touch JS that's actually being migrated/fixed in this version's pass, and re-run the full Verification Ladder (lint, tests, coverage, real browser pass) against the rewritten version — a modernized widget that silently stops firing its event handler is exactly the kind of regression step 5 of the ladder exists to catch.
7. **`static/description/index.html` (and its screenshots) is the module's de-facto acceptance spec — read it FIRST, verify against it, and regenerate its screenshots per version.** Every Odoo module ships this as its Apps Store listing page, and it's usually the only written record of what the module is *supposed to do* (each screenshot + caption is an acceptance criterion: "clicking X shows Y", "the arrow expands child rows", "user sees different permissions per company"). Three obligations follow:
   - *Before* migrating, read it and extract the documented behaviors — then verify each one clicking through the real browser flow on each version, not just "it installs and the button renders". A migration that passes every test while a documented behavior silently stopped working is a failed migration (real case: a module's whole documented purpose was dead on every version after a core mechanism changed shape, with zero errors anywhere — only checking against the listing screenshots revealed it).
   - Nothing in the automated pipeline touches this file, so it silently drifts — a screenshot can keep showing UI from three versions ago with zero test failure. Once a version's real UI-visible changes are known, regenerate the affected screenshots *from that version's own live instance* (they're per-branch files — each version branch should show its own UI generation); if nothing user-visible changed, say so explicitly in the PR rather than silently skipping the check.
   - Screenshot capture is scriptable (Playwright: login, navigate, open the widget/flow, screenshot straight into `static/description/`) — script it once, run it per version.
8. **Every app's manifest must price at €142.86 EUR or higher (`'price': ..., 'currency': 'EUR'`).** This is a floor, not a target: if an app is already listed above that (e.g. a more complex app priced higher), leave its price untouched — never lower one to hit the floor. Only raise a price that's missing or below the floor, and keep it consistent across every version branch of the same app (the price is a listing-level decision, not a per-version one).
9. **Disabling a module (`installable: False`) is not a no-op for existing databases.** Any DB where that module is already *installed* fails to load its registry entirely on the next code update ("module X: not installable, skipped" → "Some modules are not loaded" → hard failure), taking the whole instance down — it's the right flag for "can't be trusted on this version yet", but it must ship together with either an uninstall path or the explicit understanding that existing installs of it must be uninstalled/recreated. Also drop the module from every CI `-i`/`--test-tags` list on those branches, or the pipeline dies on install.
10. **When a module's core mechanism can't be honestly made to work on newer versions yet, disabling it there beats shipping it broken.** A module whose *documented central behavior* silently no-ops on the target version (per the index.html acceptance check above) should be marked `installable: False` on those branches with a manifest comment saying why and from which version, rather than migrated into a state that installs green and does nothing. Partial migration honesty is part of the deliverable.

## Environment Setup

- **One git worktree per Odoo core version being tested** (`git worktree add ../odoo-X.0 origin/X.0`) so multiple versions' servers can run concurrently without branch-switching collisions.
- **If the addon repo itself needs a different branch checked out per core version being tested at the same time, it also needs its own worktree.** A single shared checkout that you keep branch-switching will silently make one version's server run the *other* version's code — this produced a false "Odoo core bug" that was actually just stale code from a different branch still on disk.
- **pyenv/venv per Python version** the target Odoo core needs (see `references/environment-setup.md` for the version matrix).
- **Old pinned dependencies fail to build on modern macOS/arm64 toolchains** (Pillow, reportlab, psycopg2, gevent are the usual suspects). For *local* testing, don't fight the exact pin: install a newer version of just that package (prefer wheels over source builds — e.g. `Pillow==9.5.0`, `reportlab` unpinned, `psycopg2-binary`) and skip genuinely non-essential ones (`vatnumber`, `gevent`) for `--test-enable --stop-after-init` runs. **On Linux CI runners this problem mostly doesn't exist** — the original pins have prebuilt manylinux wheels; see the CI section below for why Docker sidesteps this entirely.
- **Always pass an explicit, version-distinct `--http-port`** to every `odoo-bin` invocation. Without it, if another version's server is already bound to the default port, your test's HTTP client silently talks to *that* server instead of your own — producing a "bug" that's actually a port collision, not your code. This is the single most misleading failure mode in this whole workflow: it produces a plausible, reproducible-looking symptom that has nothing to do with your changes.
- **Disable `--limit-memory-hard`/`--limit-memory-soft`/`--limit-time-cpu`/`--limit-time-real` locally** on macOS — they assume Linux resource semantics and error out.
- **A running server does not see file or data changes made while it's already up — restart it.** Two separate traps, both fixed the same way:
  - *Compiled asset bundles* (`web.assets_frontend`/`_lazy`/etc.) are cached as `ir.attachment` rows, computed once and not re-checked against the source `.js`/`.xml` on disk on every request. Edit a JS file, reload the page against an already-running server, and you're still looking at the old bundle. Templates loaded dynamically via `ajax.loadXML()` (not part of a bundle) don't have this problem — they're fetched fresh on every request, so if only *those* aren't updating, look elsewhere first.
  - *Some core caches don't see writes from a second process on the same DB.* Using a separate `odoo-bin shell` to set up or tweak data (e.g. a website's config fields) while a web server is already running against that database can leave the server serving stale values for a while, even for plain, non-computed fields — not every cache invalidates on a bare ORM write from outside its own process.
  - In both cases: kill and restart the `odoo-bin` process. Don't spend time proving *why* a specific value is stale — if you changed a file or wrote data through a different process than the one serving requests, restart first and re-test before debugging further.
- **Installing a system binary dependency (e.g. `wkhtmltopdf`) after the server already started doesn't help until you restart it.** Some availability checks run once at Python import time (module-level code, not per-request) and cache the result for the life of the process.
- **Odoo 19 flipped `--without-demo`'s default from "none disabled" (demo data loads by default in a new database) to `true` (demo data is not installed at all unless requested).** If your tests or fixtures rely on demo records (base's demo partners/products, etc.), pass `--with-demo` explicitly on 19.0+ — both locally and in CI — or they'll fail with a missing-xmlid error that looks unrelated to the actual migration.
- **Browser-script URL routing differs by version: `/odoo/...` paths (e.g. `/odoo/sales`, `/odoo/inventory/products`) exist only on ~18.0+; on ≤17.0 they 404 — use the classic hash router (`/web#id=<id>&model=<model>&view_type=form`) there.** A Playwright/tour script reused across versions needs both navigation modes behind a version switch, or the "bug" you're chasing is just a 404 page.
- **zsh does not word-split unquoted variables** (unlike bash). A flag stored in a shell variable (`PLAT="--platform linux/amd64"; docker run $PLAT ...`) arrives as ONE malformed token and the command dies with a confusing "unknown flag" error. Write flags inline, use arrays, or `${=VAR}` when scripting loops across versions in the user's default shell.

## Verification Ladder — Do All of These, In This Order, Per Version

1. **Lint clean.** Bootstrap `.pre-commit-config.yaml`/`.flake8`/`.pylintrc` from a reference OCA/Vauxoo repo of the *same Odoo-version generation* if one exists locally — copy period-appropriate config, not just the newest one. Modern lint suggestions can be flatly wrong for old Odoo APIs (e.g. `self.env._` doesn't exist before Odoo 17 — a pylint_odoo suggestion that *breaks at runtime* if blindly applied to a 13.0/14.0 branch). Verify any such suggestion against the actual target version's core before applying; if inapplicable, revert and justify a scoped `# pylint: disable=...` with a one-line comment citing why.
2. **Scoped test run.** `-i <modules> --test-enable --test-tags=/<module1>,/<module2>,...` on a *fresh* database — scope test-tags to just your modules for fast iteration.
3. **Full untagged run at least once.** No `--test-tags` — makes sure nothing else broke. Read the log for FAIL/ERROR lines specifically attributable to *your* modules; core Odoo's own meta-tests (tests that intentionally raise errors to verify the test runner's own error logging) and pre-existing environment gaps (missing JS test harness, substituted reportlab) are expected noise, not your bug.
4. **Measured coverage**, not a test count. `coverage run --source=<your modules> --omit=*/tests/* -- <odoo-bin invocation>` then `coverage report -m`. Read the *Missing* column and close real gaps with real tests — controller routes nobody calls, cron methods, exception branches, batch loops that no-op on an empty recordset. "I wrote N tests" is not coverage; the percentage and the missing-lines list are.
5. **Real headless-browser pass, with Odoo's own JS tours as the default mechanism, against a running instance.** Write a tour (`registry.category("web_tour.tours").add("<name>", {steps: () => [...]})`) covering the real interactive flow (click add-to-cart, submit a form, open a modal), driven by a normal `HttpCase` Python test calling `self.start_tour("/", "<name>", login="admin")`. Because it's a normal test, it's already inside the scoped/full test runs and the CI pipeline this skill sets up — no separate script or extra CI step needed. Check console/page state on *every* representative page — home **and** a listing/detail page, not just the homepage. A template injected via an xpath that only renders on `/` and silently vanishes on `/shop` is a real, seen-in-production bug class (see table below).
   - **The headless Chrome console a tour runs in is not a black box — read it.** Odoo pipes every `console.log`/`warn`/`error` call made during the tour into the test log itself, through a `<test>.browser` child logger (`ChromeBrowser._handle_console` in `odoo/tests/common.py`) — grep the *full* test log for it, not just the pass/fail summary line, the same way this skill already insists on reading logs instead of trusting exit codes elsewhere. A single `console.error` auto-fails the test *and* auto-saves a screenshot before doing so.
   - **Pass `--screenshots <dir>`** so you know exactly where that failure screenshot lands (Odoo saves one automatically either way, into a temp dir if you don't), and **actually open the PNG** — don't infer what broke from the exception text alone.
   - **Pass `--screencasts <dir>`** for a literal "watch it with your own eyes" artifact: Odoo records a real video of *every* `start_tour()` run, pass or fail, to `<dir>/<db_name>/screencasts/` — play it back for the specific interaction you're verifying instead of trusting a green result.
   - **Keep a standalone Playwright (or equivalent) pass as the fallback**, for checks a tour's own step/assertion model genuinely can't reach — inspecting a third-party JS payload (e.g. intercepting a `dataLayer.push` call before a page navigates away), multi-tab/multi-window scenarios, or anything needing to observe raw network traffic.
   - **Zero console errors is not the same as "the feature works," in a tour or in Playwright.** A UI element that silently fails to render (a missing condition, a class collision with something else on the page, a field that no longer exists) throws *no* JS error at all — it's just absent. A tour step that "completes" because its target selector silently matches nothing is the tour-framework version of this same trap. For every feature under test, explicitly assert the thing is *present* (the button exists, the modal opens, the expected text shows), not only that nothing crashed.
   - **A blocked or empty result from a click can mean the test raced a real page transition, not that the code is broken.** Add-to-cart/remove-from-cart flows sometimes do a full page reload on older Odoo versions and pure AJAX on newer ones (this differs *between* target versions, not just between apps) — if you read page/JS state right after a click and get nothing, check whether a navigation happened before concluding the handler never fired; prevent the navigation (block the request, stub `form.submit`, or add a synchronous `preventDefault` listener) and retry before calling it a bug.
   - **Exercise the flow on records in every relevant business state, not just freshly-created drafts.** Imperative data-population bugs (a backfill/onchange that only covers `state='draft'`) are invisible on the draft records a quick test creates and only surface on confirmed/sent/done ones — precisely the records real users look at. Open at least one confirmed/completed record per documented surface.
   - **Interact PAST the first render: expand/collapse, close via outside click, then navigate to a different record and look back.** Overlay widgets (popovers/tooltips/dropdowns) attached to `<body>` outlive the widget that created them; double-bound handlers make toggles undo themselves instantly; none of this shows up in a "button renders, popover opens" smoke check.
6. **If the app integrates with an external tool via a DOM-selector- or URL-pattern-based config it owns (a Google Tag Manager container export, similar tag-manager JSON, a browser extension config, etc.), audit that config file separately from the module's own code.** These configs reference CSS selectors, element ids, and URL patterns that can go stale exactly like inline `data-*` attributes do, and no amount of Python/XML migration touches them — the module can pass every test while the external config still points at DOM state that hasn't existed since two versions ago. Rebuild references to selectors/URLs confirmed still valid on the target version's real DOM; where the app's own JS already computes a stable value (an event name pushed to a JS-level event bus, a data attribute assembled from a core helper), prefer wiring the config to that instead of re-deriving another DOM hook that can drift again.
7. **Re-verify after every fix, against the original failing scenario.** A one-line change that "looks like" the fix (e.g. reformatting an XML comment because it seemed like the plausible cause) must be proven against the *exact* originally-failing page/action before it's called done. Don't declare victory on a plausible-sounding cause without reproducing the fix end-to-end.
8. **CI: run the same verification on every push, in Docker.** See below.

## CI Pipeline — GitHub Actions Inside the Official Odoo Image

If the user is on GitHub's free tier (most common for a personal repo), GitHub Actions is enough — no external CI needed. Two approaches were tried on this migration; only the second one is worth using:

- ❌ **Clone Odoo core fresh + `pip install -r requirements.txt` on every run.** Works (old pins do have Linux wheels), but slow, and fragile the moment any of those decade-old pins stop resolving.
- ✅ **Run the job inside the official `odoo:<version>` Docker Hub image.** It already has Odoo, every dependency, and `python3-pip` installed via its `.deb` package, with community addons bundled in. A run only needs to check out the repo, `python3 -m pip install coverage` (the image has no bare `pip` executable — only reachable via `python3 -m pip`, same for the `coverage` command afterward), and point `--addons-path` straight at the checkout. No separate core clone, no `requirements.txt` install, no macOS-only dependency substitutions to worry about (they were only ever needed on macOS/arm64 — the Debian-based image sidesteps the whole problem).

Key details that aren't obvious until you hit them:

- Use `container: {image: "odoo:X.0", options: "--user root"}` on the job — the image's default `odoo` user can't `pip install` system-wide.
- When the job itself runs in a container, service containers (Postgres) are reached **by their service name** (e.g. `postgres`), not `localhost` — that's how Actions networks containerized jobs together. Don't reuse a `localhost`-based Postgres config from a non-containerized job.
- `odoo-bin`'s own exit code is not reliable across versions for signaling test failures — grep the log for the same markers the local verification ladder reads by hand (`modules.module: Module .*: N failures, M errors` with N or M non-zero, `tests.runner: N failed, M error(s)`, or any `FAIL:` line) and `exit 1` explicitly if found.
- Publish the coverage report to the run's job summary (`>> $GITHUB_STEP_SUMMARY`, wrapped in a fenced code block) — visible in the Actions UI with zero extra services (Codecov etc.) needed.
- **On newer `odoo:X.0` images (18.0/19.0+), `python3 -m pip install coverage` fails outright with `error: externally-managed-environment`.** These images moved to a Debian base with Python 3.12, which enforces PEP 668. Add `--break-system-packages` to the pip install step — it's safe here since the container is thrown away at the end of the job, not a persistent system Python.
- **Name the workflow file version-specifically from the very first commit** (`tests-X.0.yml`, never a shared `tests.yml` reused across branches). GitHub Actions caches and displays a run's workflow name keyed by file *path* — if every version branch reuses the same path, Actions can show a stale/wrong Odoo version name in the Actions UI for runs on a different branch, even though the underlying job is correct. This is easy to miss because the job itself passes; only the displayed name is wrong.
- See `references/github-actions-odoo.yml` for a complete, copy-adjustable workflow.

## Common Bug Classes Found This Way

| Symptom | Real Cause | Fix |
|---|---|---|
| Feature works on `/` but nowhere else | Injected via a template/xpath that only applies to a subset of routes (e.g. `portal.frontend_layout`'s `//nav`, which some listing routes don't render through) | Move the injection to a point proven to render on every route (e.g. `web.layout`'s `<head>`) |
| `$ is not defined` (or similar) in the browser console | Inline `<script>` on `DOMContentLoaded` assumes jQuery/a lib is already loaded, but Odoo lazy-loads part of the asset bundle | Rewrite as a `publicWidget` — it only attaches once its declared dependencies are actually loaded |
| A "new feature" flag/field exists but never does anything on the frontend | The access-control method (e.g. `can_access_from_current_website`) still reads the *old* single-value field a mixin defines, while the app only ever populates the *new* multi-value field it added | Override the access method to check the new field |
| A `NameError` buried in an `except` clause that never seems to fire in normal use | Exception class referenced without being imported — only surfaces under the exact race condition the `except` exists to handle | Add the import; also check except-clause order (specific subclasses before their parent, e.g. `OperationalError` before `Error`, or the parent swallows everything first) |
| A modern lint suggestion breaks at runtime on an old branch | The suggested API was added in a *later* Odoo version than the branch targets | Verify the suggestion against the actual target version's core before applying; if inapplicable, revert with a justified, scoped disable comment |
| `.pyc`/`__pycache__` show up in a diff | The repo's `.gitignore` doesn't cover them (or only covers them going forward) | Add the standard ignore section, `git rm --cached` anything already tracked, and say so explicitly in the PR — it's a removal, not new junk |
| CI step fails with `pip: not found` (or similar) inside a vendor Docker image | The image only exposes `python3 -m pip`, not a bare `pip` executable | Invoke via `python3 -m pip` / `python3 -m coverage` instead of the bare command |
| A test passes locally but not in CI (or vice versa) with an identical assertion | Environment mismatch invisible from the assertion itself — e.g. a different demo dataset shape, a public/portal user lacking read access to a record the test created without publishing it (`website_published`), or a route requiring a parameter your test omitted | Reproduce the *exact* failing request/record state, don't just re-read the assertion; check auth/access rules and required params first |
| A feature that used to work is now dead with zero errors anywhere — no exception, no console error, condition just never true | Core didn't rename the field/mechanism the app's condition depends on, it *replaced* it with a differently-shaped one (e.g. a 3-value select field replaced by a boolean plus a separately-computed float, or a `virtual_available`-style field dropped from the data payload entirely) — the old field name is simply absent from the new context, so the check is unconditionally false | Diff the *actual* new fields/shape in the target version's core (the model, and what the equivalent core template/JS now reads) against what the app's condition assumes; redesign the condition around the new shape, don't just search-and-replace the field name |
| An element renders once (confirmed via a direct render call or a log) and then is gone from the live DOM a moment later, no error anywhere | It shares a CSS class/id naming convention with something core's *own* JS explicitly searches for and removes as part of its own re-render/cleanup cycle (e.g. `$(...).find('.some_prefix_' + id).remove()` before injecting a fresh message) — the app's element matches that selector and gets swept up in the same cleanup, especially likely if the app's original author copied the naming convention from core in the first place | Grep the target core's JS for `.remove()`/`.empty()` calls near the class prefix in question; rename the app's element to a prefix core never searches for |
| A "native" core event/hook that used to carry the data you need now fires only sometimes, or never, depending on unrelated settings | The core JS that dispatches it is gated behind a *different* feature's config field (e.g. a native "Google Analytics" integration setting, unrelated to the app's own similarly-named key) — sites that don't have that other feature configured never get the trigger, even though the app's own settings are correct | Trace back to *where* core actually dispatches the event/sets the data, not just where it's consumed; if the underlying data is computed unconditionally elsewhere (a data attribute rendered server-side regardless of the gate) read that directly instead of depending on the gated trigger |
| JS still looks like it did on the original version, several migrations later | Each version's migration ported the frontend code forward unchanged because "it still works" — nobody checked what the target version's own core had already moved to | Per Migration Strategy step 6: match whatever pattern the target version's own core uses for the same kind of surface (`publicWidget`/OWL, ES6 `@odoo-module`, vanilla JS over jQuery where core's equivalent already made that switch) |
| An xpath targeting a `class` attribute stops matching after a version bump, with no other change to that element | Core rewrote the template to use `t-attf-class`/`t-att-class` instead of a literal `class` — Odoo's `hasclass()` xpath helper (`ir_ui_view.py`) only ever reads the literal `class` attribute, never a dynamic one | Match on `contains(@t-attf-class, '...')` (or the relevant dynamic attribute) instead of `hasclass()` |
| A widget's whole JS file throws at module-load time (`.include()` on `undefined`), and the feature it adds silently never appears anywhere | Core replaced a legacy `publicWidget.registry.*` entry this app extended with a new Interaction class (`@web/public/interaction` + a specific module's `interactions/*.js`) — the old registry entry simply doesn't exist anymore | Rewrite the extension using `patch()` (`@web/core/utils/patch`) on the Interaction class's prototype, chaining through `super.<method>(...)`, matching the pattern the target version's own core modules use for the same class of widget |
| A core event handler (`add_to_cart_event`, `view_item_event`, etc.) that used to carry extra data now fires but the data is missing, with zero console errors | Core switched from jQuery's `$el.trigger(name, [extraArgs])` to a native `element.dispatchEvent(new CustomEvent(name, {detail}))` — jQuery still delegates native custom events to `publicWidget`'s events map, but the payload no longer arrives as extra positional arguments (`Array.prototype.slice.call(arguments, 1)` returns empty) | Read `ev.originalEvent.detail` instead of extra handler arguments |
| Combining a domain built with `expression.AND()`/`OR()` and a domain from an overridden core method now crashes with something like `unsupported operand type(s) for &: 'DomainAnd' and 'list'` | Core replaced the old list/tuple domain representation with a `Domain` AST class (`odoo.fields.Domain`) — the overridden method's `super()` call now returns a real `Domain` object, not a plain list, and `expression.AND()` doesn't know how to combine the two | Wrap the app's own domain in `Domain(...)` (accepts old-style `'|'`-prefixed tuple syntax) and combine with `&`/`|` operators instead of `expression.AND()`/`OR()` |
| A route that used to render fine now 404s or redirects unexpectedly for a scenario that looks unrelated to what actually changed this version | A new `ir.rule` added this version hides records with a specific empty/unpublished state from public or portal users entirely (e.g. a category with zero published products) — the route's own logic (auth gate, redirect, etc.) never even gets reached because the record lookup itself fails access first | Reproduce with a record that actually satisfies the new rule (e.g. give the test category a real published product) to isolate the scenario actually being tested from the new access restriction |
| A module's Apps Store listing page (`static/description/index.html`) still shows a Settings screen, button, or field from several versions ago | Nothing in the automated pipeline touches this file — it's marketing HTML with static screenshots, invisible to tests, lint, and coverage alike | Per Migration Strategy step 7: once a version's real UI changes are known, check every screenshot against the current UI and regenerate any that have drifted |
| A field/button only shows on some records; on confirmed/sent/done ones it's just absent, forever, with no errors | The field is populated imperatively by several mechanisms that drifted apart (an onchange + a `create()` override + an install-hook backfill whose raw SQL filters by `state`) — records outside the covered states never get the value and nothing ever recomputes it | Convert to a real computed field (`compute=`, `store=True`, `@api.depends(...)`) and delete the imperative writers; confirm via the consumer (JS/view) what the *actual* dependencies are before writing the `@api.depends` |
| After converting an existing plain field to `compute=` + `store=True`, old rows stay NULL after upgrading — only new records compute | Odoo's `_auto_init()` only auto-recomputes a stored compute field when its DB *column is newly created* (`Field.update_db()` returns `not column`); a column that already existed (it was a plain field before) is skipped entirely | Keep/rewrite an explicit `post_init` hook (and an upgrade `migrations/` script for existing installs) that calls the real compute method on all rows — no state filters — plus an explicit `flush_recordset()`/`flush()`: the compute alone only fills the ORM cache, and same-transaction raw-SQL reads won't see it |
| A module's documented central behavior silently no-ops from some version on — installs green, tests green, does nothing | Core *replaced the mechanism* the module's condition reads without renaming anything: e.g. the UI action that used to write a DB column (company switcher writing `res.users.company_id` pre-13.0) became session/context-only, so raw-SQL reads of that column no longer reflect what the user selected | Trace what the *target version's* UI action actually writes/reads (grep the switcher/action's own JS+controller in core) and re-anchor the module on that; if it can't be honestly re-anchored yet, `installable: False` with a dated comment (Strategy step 10) beats shipping it dead |
| Inline `<script>`/`onclick=` inside popover/tooltip HTML content does nothing from some version on, silently | Bootstrap ≥ 4.3's sanitizer strips script tags and `on*` attributes from popover/tooltip content before injection — no error, the handlers are just gone | Move behavior out of the HTML string into real delegated JS handlers bound after render (`shown.bs.popover` etc.); never ship logic inside popover content markup |
| An overlay (popover/tooltip/dropdown) survives navigating to another record and floats over the wrong screen | The overlay element is appended to `<body>`, so destroying the widget that created it doesn't remove it; nothing else owns it | Dispose the overlay in the widget's `destroy()` and register any document-level (outside-click) handlers with a per-instance event namespace so they're removable |
| Clicking an expand/toggle briefly opens then instantly closes (or handlers seem to run twice) | The same delegated handler got bound from more than one lifecycle callback (e.g. `shown.bs.popover` *and* an RPC-refresh `.then()`), and again on every reopen — one click runs the toggle N times | Make handler-binding idempotent: namespaced events, `.off('click.ns')` before `.on('click.ns', ...)` — always, in any `_bind*Handlers`-style helper |
| A popover/modal table stretches across the whole screen with huge dead gaps between columns | Bootstrap's `.table` class forces `width: 100%`, and the overlay container itself has no width constraint — so column widths are distributed over all available space | Content-size it: `table { width: auto }`, `white-space: nowrap` cells, right-aligned numeric columns with a small `min-width`, and a sane `max-width` on the overlay container |
| A PR that was fine reports conflicts later (or from the start) even though "nothing touched the base" | The version target branch was cut from a commit that is *not an ancestor* of the work branch (e.g. from a sibling branch's later tip), so both sides carry different versions of the same files | Always cut the target branch at the merge-base with the work branch; if already diverged and the work branch's tree is the complete intended state, `git merge -s ours <base>` on the head (documenting why in the merge commit) resolves it without risking content |

## PR/MR Discipline

- Split commits by concern (tooling bootstrap / one commit per bug fixed / test coverage / CI), each message stating the user-visible symptom, the root cause, and how it was verified — not just what changed.
- Never push directly to the version branch; work branch → push → PR/MR, left open for review.
- After pushing, leave a real local instance of that version running (with the fixed modules installed) so the requester can test it themselves without re-running anything.
- If a fix turns out to be wrong or incomplete once verified further (it happens — see the bug table's "plausible-sounding cause" trap), say so plainly in a follow-up commit rather than quietly amending history.

## References

- `references/environment-setup.md` — concrete pyenv/worktree/pip-substitution recipes and the Odoo-version → Python-version matrix.
- `references/github-actions-odoo.yml` — a complete, copy-adjustable GitHub Actions workflow (Docker-based, tests + coverage).

## Example: Resuming a Multi-Version Migration in a Fresh Session

This skill triggers on the phrasings in the frontmatter, but a cold session
has no memory of prior version branches, PRs, or running instances — carry
that context explicitly. This works for any repo and any version range
(there's nothing 13.0/14.0/15.0-specific about the flow itself); fill in
the bracketed placeholders with your own repo path, version numbers, app
names, PR numbers and ports:

```text
Use the agents-oscarolar:odoo-legacy-app-migration skill to continue
migrating /path/to/my-addon-repo to Odoo <target>.0.

Context: <prev2>.0 (PR #<n>, branch work-<prev2>.0) and <prev1>.0
(PR #<n+1>, branch work-<prev1>.0) are already migrated following this
methodology, with instances running on localhost:<port1> and
:<port2> respectively. The apps are: app_a, app_b, app_c, app_d.

Create a work-<target>.0 branch from origin/<target>.0 (it exists, but
may have the same half-disabled-apps pattern earlier versions started
with). Per app: audit its real state on that branch (don't trust
`installable`), migrate/fix whatever's needed, port or add tests,
close coverage to 100% where reasonable, and verify with a real
browser (a JS tour by default, per the Verification Ladder) on more
than one page (not just the homepage). Add the Docker-based GitHub
Actions pipeline (odoo:<target>.0, workflow file named
tests-<target>.0.yml) same as on the prior versions. Open a PR without
merging directly, and leave the instance running on a port distinct
from every version already running when done.
```

This same template also covers the very first version of a migration
(drop the "Context" paragraph entirely — there's nothing prior to carry
forward) and a single-step next-version continuation (just one prior
version in "Context" instead of two).
